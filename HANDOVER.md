# 引き継ぎドキュメント（HANDOVER.md）

## 1. 目的とゴール
- **当初の目的**: 昨今の金利上昇を背景に、住宅ローン（変動金利）の変動によって「支払い総額の差分」「月々の支払額の内訳」「未払利息」の発生状況を視覚化できるシミュレーションツールの作成。
- **最終ゴール**: ユーザーが夫・妻それぞれのペアローン条件を入力し、世帯合算での推移をグラフで直感的に確認できるモダンなWebアプリを構築・提供すること。

## 2. 現在のコンテキストと前提条件
- **技術スタック**: React 19, TypeScript 6, Vite 8, Tailwind CSS 4, Recharts 3, lucide-react, React Hook Form 7, Zod 4, @hookform/resolvers 5
- **環境・制約**: `tasks/loan-simulator` ディレクトリ配下にて作業中（`.gitignore` で除外されている）。
- **主要な関連ファイル**:
  - `src/lib/loanCalculator.ts`: 住宅ローンの計算エンジン（5年ルール、125%ルール、未払利息の計算ロジック）
  - `src/lib/loanMerger.ts`: ペアローンの個別計算結果を世帯合算するロジック
  - `src/lib/utils.ts`: `calculatePayment()`（元利均等返済の月額計算）などのユーティリティ関数
  - `src/ts/modelInterfaces.ts`: `LoanConditions`, `MonthlyDetail`, `InterestRateScenario` の型定義
  - `src/schema/zodSchema.ts`: フォームバリデーションスキーマ（`formSchema`, `personSchema`, `customNumber`）
  - `src/components/LoanForm.tsx`: フォーム全体の管理（RHF + zodResolver）、`toLoanConditions()` によるフォーム値→計算エンジン入力の変換
  - `src/components/PersonalLoanForm.tsx`: 夫・妻それぞれの個人ローン入力フォーム（LoanFormから分離）
  - `src/components/LoanChart.tsx`: Rechartsによる月々の内訳・残高推移の描画コンポーネント
  - `src/components/PaymentHistory.tsx`: 月別明細の表示コンポーネント
  - `src/components/TooltipIcon.tsx`: ツールチップアイコンコンポーネント
  - `src/App.tsx`: フォームとグラフを統合・制御するメインコンポーネント

## 3. ここまでの経緯（タイムライン）

### フェーズ1〜4（前回セッション以前）
- 基本的な計算エンジンの作成からグラフ描画、ペアローンUIの統合、ドキュメント化までを完了。
- 「支払額を固定する（繰り上げ返済シミュレーション）」機能の分離、合算時の金利表示調整、Zodバリデーション追加等の改修を実施。
- Playwright MCP を用いたE2E検証を完了。

### フェーズ5（今回セッション: リファクタリング・コードレビュー・新機能実装）

#### 5-1. LoanForm.tsx のリファクタリング
- **PersonalLoanForm.tsx の分離**: 夫・妻の個人フォーム部分を `PersonalLoanForm.tsx` として独立コンポーネントに切り出し。
- **zodSchema.ts の分離**: Zodスキーマ定義を `src/schema/zodSchema.ts` に移動（`formSchema`, `personSchema`, `customNumber`, `scenarioSchema`）。
- **型定義の整理**: `FormValues`, `PersonValues` を zodSchema.ts からエクスポート。

#### 5-2. zodSchema.ts の修正
- `customNumber` の `maxVal` 条件: `if (maxVal && maxMsg)` → `if (maxVal !== undefined && maxMsg !== undefined)` に修正（`maxVal = 0` の場合に falsy 判定されるバグを防止）。
- `z.nan()` のコメント追記: `fixedPaymentAmount` の union 型で `z.nan()` が必要な理由（固定モード OFF 時に空欄 input が返す NaN をスキーマ段階で通過させ、superRefine で状態判定する設計）を詳細に記述。
- `superRefine` 内の条件: `!data.fixedPaymentAmount` → `data.fixedPaymentAmount === undefined` に修正（falsy チェックでは `0` が誤判定される可能性があるため、明示的な undefined チェックに変更）。
- Zod エラーコード: `z.ZodIssueCode.custom`（非推奨警告）→ `code: "custom"` に変更。

#### 5-3. コードコメントの整備
- `FieldErrors<FormValues>` のネスト構造に関する解説コメントを `PersonalLoanForm.tsx` の `personErrors` 定義箇所に追記。RHF が FormValues の型構造から自動的にネストしたエラー型を構築するため、zodSchema 側に特別なキー定義は不要である旨を明記。
- 全ファイルにわたるコードコメントの包括的なレビューと修正を実施。

#### 5-4. `initialMonthlyPayment` の新規実装（月々の支払額のユーザー入力値を計算に反映）

**背景**: フォームの「月々の支払額」フィールド（`monthlyPayment`）は表示のみで、`toLoanConditions()` で計算エンジンに渡されていなかった。

**設計判断**: 既存の `customMonthlyPayment` は「初期返済額の上書き」＋「5年ルール見直しの無効化」の2つの責務を持つため、流用せず新フィールド `initialMonthlyPayment` を追加して責務を分離した。

| フィールド | 初期返済額の上書き | 5年ルール見直しの無効化 |
|---|---|---|
| `initialMonthlyPayment`（新規） | ✅ | ❌（通常適用） |
| `customMonthlyPayment`（既存） | ✅ | ✅（無効化） |

**変更ファイル**:
1. `src/ts/modelInterfaces.ts` L10: `initialMonthlyPayment?: number` フィールド追加
2. `src/lib/loanCalculator.ts` L48-56: 初期返済額の決定に `else if (initialMonthlyPayment)` 分岐追加。優先順位: `customMonthlyPayment`（固定モード） > `initialMonthlyPayment`（ユーザー指定） > `calculatePayment()`（元利均等返済の公式による算出、フォールバック用）
3. `src/components/LoanForm.tsx` L111-112: `toLoanConditions()` に `initialMonthlyPayment: d.monthlyPayment` 追加
4. `src/components/PersonalLoanForm.tsx`: コードコメントの更新（旧TODO削除、実装済み説明追記）、UI注釈テキストに「条件変更時はこの値も合わせて修正してください」を追記

**既知の制約（将来の改善候補）**: `monthlyPayment` の初期値は `defaultValues` で一度だけ計算され、借入額・金利・期間を変更しても自動更新されない。条件変更後にユーザーが `monthlyPayment` を手動修正しないと、不適切な返済額でシミュレーションが実行される（例: 借入額増加時に未払利息が急増する結果になる）。将来的に `useEffect` による自動再計算の追加が検討できるが、「ユーザーが手動で変更した値を自動上書きしてよいか」というUX判断が必要。

## 4. 決定事項と確定済みの仕様
- **計算ロジックの正当性**: 途中で金利が上昇した際、一時的に「前年より利息支払い額が増加する」ことはローンの仕組み上正しい挙動として確定済み。
- **ペアローンの合算方式**: 夫・妻それぞれで独立して `MonthlyDetail[]` を計算し、最後に `mergeMonthlyDetails` を使って月ごとに値を合算（加算）して描画するアプローチで確定。
- **グラフの年次集計**: 360ヶ月（30年）のデータをそのまま描画するとグラフが潰れるため、`LoanChart.tsx` 側で12ヶ月ごとの「年間合算データ」と「年末時点の残高」にサンプリングして描画する仕様で確定。
- **ペアローンの金利**: 同時期に借入するケースがほとんどのため、合算時は同じ金利適用として表示。異なる金利の場合は「-（個別参照）」と表示。
- **`initialMonthlyPayment` と `customMonthlyPayment` の共存**: 固定モード有効時は両フィールドが同時に設定されるが、`customMonthlyPayment` が常に優先される（`initialMonthlyPayment` は無視される）。これは仕様通り。
- **zodSchema の `maxVal` 条件**: `if (maxVal !== undefined && maxMsg !== undefined)` で統一（falsy チェックは禁止。`0` が有効値となるケースに対応）。
- **zodSchema の `superRefine` 内条件**: `data.fixedPaymentAmount === undefined` で明示的に判定（`!data.fixedPaymentAmount` は `0` を誤判定するため禁止）。

## 5. 現在の状態と次期タスク
- **現在の状態**: フェーズ7（網羅的E2Eテスト再実施）が完了。E2Eテスト29件（本テスト26件＋追加テスト3件）全PASSを確認。ビルド（`tsc -b && vite build`）も正常通過。
- **次期タスク: ユニットテスト作成（フェーズ8）**
  - **目的**: E2E偏重のテスト構成を補完し、計算ロジックの正確性をユニットテストで担保する
  - **技術スタック**: Vitest + @testing-library/react
  - **対象モジュール**: `loanCalculator.ts`, `utils.ts`, `loanMerger.ts`, `usePaymentHistoryHooks.ts`
  - **詳細**: セクション10「ユニットテスト作成プラン」を参照

> **参考メモ（将来の改善候補、今回のスコープ対象外）**
> - `monthlyPayment` の自動再計算（`useEffect` 追加、UX判断が必要）
> - 125%ルールの適用オン/オフ機能（一部ネット銀行の「新変動金利型」対応）
> - ボーナス返済への対応（年2回のボーナス払い併用ロジック）
> - 端数処理によるUX改善（残高閾値導入で「✅完済」扱いにする等）
> - `calculatePayment()` の金利0%時ゼロ除算ガード追加

## 6. テストシナリオ

以下は今回のセッションで実施した修正・新規実装を網羅的に検証するためのテストシナリオ。
dev サーバー（`npm run dev`）を起動し、ブラウザまたは Playwright でフォーム操作→計算結果を確認する。

### A. `initialMonthlyPayment` の基本動作

| # | テストケース | 手順 | 期待結果 |
|---|---|---|---|
| A-1 | 通常モード・monthlyPayment 未変更 | デフォルト値のまま「再計算する」ボタン押下 | 以前と完全に同じ計算結果（グラフ・テーブルの数値が一致）。`initialMonthlyPayment` = `calculatePayment()` の結果なので差異なし |
| A-2 | 通常モード・monthlyPayment を増額 | monthlyPayment を例えば 60,000円に変更して再計算 | 初期返済額が 60,000円で計算が実行される。元金充当が増え、返済期間が短縮（完済が早まる）。5年ルール・125%ルールは通常適用 |
| A-3 | 通常モード・monthlyPayment を減額 | monthlyPayment を例えば 30,000円に変更して再計算 | 初期返済額が 30,000円で計算が実行される。金利次第で利息が返済額を上回り未払利息が蓄積する可能性がある |
| A-4 | 固定モード有効時 | fixedPaymentEnabled をON、fixedPaymentAmount を 70,000円に設定して再計算 | `customMonthlyPayment = 70,000` が優先され、`initialMonthlyPayment` は無視される。5年ルールの見直しも無効化。既存の固定モードと同じ挙動 |
| A-5 | 固定モードOFF→ON→OFF | 固定モードを切り替えながら再計算 | OFF時は `initialMonthlyPayment` が使用され5年ルール通常適用。ON時は `customMonthlyPayment` が優先。状態の切り替えで不整合が発生しないこと |

### B. 条件変更時の monthlyPayment 非連動（既知の制約の確認）

| # | テストケース | 手順 | 期待結果 |
|---|---|---|---|
| B-1 | 借入額変更後に monthlyPayment 未修正 | 借入額を 1810万→3000万に変更、monthlyPayment は触らずに再計算 | 計算自体は正しく実行されるが、3000万に対して旧額（約48,614円）は不足 → 未払利息が蓄積する結果が表示される |
| B-2 | 借入額変更後に monthlyPayment を適切に修正 | 借入額を 3000万に変更、monthlyPayment も適切な額（例: 約80,000円）に修正して再計算 | 正常な返済シミュレーションが表示される |
| B-3 | UI注釈テキストの表示確認 | 月々の支払額フィールドの下部を確認 | 「※借入額や金利・期間を変更しても、この値は自動更新されません。条件変更後はこの値も合わせて修正してください。」がアンバー色で表示される |

### C. zodSchema の修正検証

| # | テストケース | 手順 | 期待結果 |
|---|---|---|---|
| C-1 | 固定モードON・固定額未入力 | fixedPaymentEnabled をON、fixedPaymentAmount を空欄のまま再計算 | 「固定モードが有効な場合、固定額を入力してください」エラーが表示される |
| C-2 | 固定モードOFF・固定額未入力 | fixedPaymentEnabled をOFF、fixedPaymentAmount を空欄 | エラーなし（superRefine の条件分岐で固定モードOFF時はスキップされる） |
| C-3 | 借入期間の範囲バリデーション | termYears に 0 または 51 を入力して再計算 | それぞれ「1以上を指定してください」「最大50年です」エラーが表示される |
| C-4 | monthlyPayment に 0 を入力 | monthlyPayment を 0 に変更して再計算 | 「1円以上を指定してください」エラーが表示される |

### D. リファクタリングの回帰テスト

| # | テストケース | 手順 | 期待結果 |
|---|---|---|---|
| D-1 | 夫のフォームリセット | 夫のフォームの「リセット」ボタン押下 | 夫のフォームのみが初期値に戻り、妻のフォームは変更されない。リセット後に自動再計算が走る |
| D-2 | 妻のフォームリセット | 妻のフォームの「リセット」ボタン押下 | 妻のフォームのみが初期値に戻り、夫のフォームは変更されない |
| D-3 | 金利変動シナリオの追加・削除 | シナリオを2つ追加→1つ削除→再計算 | 残った1つのシナリオが正しく計算に反映される |
| D-4 | ビルド通過 | `npm run build` を実行 | `tsc -b && vite build` がエラーなく完了 |

## 7. ~~次のAIへの初期プロンプト（フェーズ6向け、完了済み）~~
> ~~フェーズ6のE2Eテスト実施用プロンプト。フェーズ6・7で完了済みのため無効。~~
> 現在有効なプロンプトはセクション10-7を参照。

## 8. 実施済みテストの詳細（フェーズ6：総合検証）

### ユーザープロンプト
```markdown
## タスク： 住宅ローンシミュレーションのテスト実施
`tasks/loan-simulator`はReact, TS, Viteで構築されたペアローン対応の住宅ローンシミュレーターです。詳しくは当該プロジェクトのREADMEに記載していますが、住宅ローンの金利上昇・金利変動が「支払い総額」や「月々の支払内訳」、「未払利息」にどのような影響を及ぼすかをシミュレーションできるものです。

### 仕様について
README.mdの「設計のポイント（計算ロジック）」と「主要ファイル構成」を確認してください。

## 前提・背景情報
以前途中までテストを実施していましたが、途中で設計上の瑕疵が見つかったため急遽追加実装やリファクタリングを行いました。
設計上の瑕疵とは「月々の支払額が設定されていなかった」という致命的なものです。
そこで今回、改めて当該プロジェクトの網羅的なテストを実施してもらいたいと考えています。

### 以前途中までのテストデータ（関連ファイル）
- tests/e2e/run_test.js
- tests/ 配下の各種ファイル

#### 参考ファイル
- tasks/loan-simulator/HANDOVER.md（※実装したAIからのテストシナリオの指示が書いてあります）

## テスト希望内容
以前途中までのテストデータ（関連ファイル）の内容や参考ファイルの内容を筆頭に、以下をお願いしたいと思います。各テストは暗黙的に、数値以外にもテーブル表示やグラフ描画のチェックも含むものとします。
※内容的な重複があるかもしれませんので、その場合は適宜取捨選択してください

- ペアローンおよび夫婦別（シングル想定）において仕様に則った適正・適切な挙動になっているか
    - シングルまたはペアローン含めて多数の現実的に想定しうる数値を入力した仮実地テスト
- 途中で金利が上昇するケース
- 極端な繰り上げ返済（増額固定）を行うケース
- 繰り上げ返済（固定モード）を行うケース
- ユーザーの金額指定返済（手動入力モード）を行うケース
- わざと未払利息が発生するような数値入力（金利）にした挙動テスト
    - 例：極端な低月額返済を行うケース（未払利息の発生確認が目的）
- 支払最終年で一括精算分がある場合と無い場合のシミュレーション
- そもそもローンシミュレーターとして適切か
- その他、今回の実装においてAIが必要と考えるテストシナリオ（上記ユーザー考慮漏れを網羅的にカバー）
```

### 8-1. テストのアプローチと思考プロセス
フェーズ5の完了を受け、本セッション（フェーズ6）では、設計上の瑕疵（月額の未設定による5年ルールの適用漏れなど）が完全に修正され、ローンシミュレーターとして正しく機能しているかを最終検証しました。
手動でのブラウザ確認に加え、**Playwrightを用いた包括的なE2Eテストスクリプト（`tests/comprehensive_e2e_test.mjs`）を独自に作成**し、ローカル開発サーバー（`npm run dev`）に対して自動テストを実行するアプローチを採用しました。

**【独自スクリプト作成の理由】**
- `run_test.js` や `QA_Test_Scenarios.md` に記載された既存のテスト内容は部分的なものであり、ユーザーが求める「網羅的なテスト（多数の数値入力、未払利息発生、一括清算、繰り上げ返済など）」を自動化し、冪等性のある検証環境として残すため。

### 8-2. 検証シナリオと結果
スクリプトを通して以下のシナリオを検証し、すべてのロジックが期待通り動作することを実証しました。

1. **シングル/ペアローン 現実的数値の挙動**: 一般的な借入額・金利でエラーなく計算され、グラフ・テーブルが正しく描画されること。
2. **途中で金利が上昇するケース**: 経過月（例：61ヶ月目）に金利が急上昇するシナリオを追加し、125%ルールと未払利息の蓄積ロジックが発動すること。
3. **極端な繰り上げ返済（増額固定）**: 月々の支払額を通常より高く固定した場合、5年ルールが無効化されて元金が急速に減少し、期間が短縮されること。
4. **ユーザー金額指定（monthlyPayment 指定）**: 月々の支払額を手動上書きした場合、その額が「初期返済額」として優先採用されること（フェーズ5の新機能の検証）。
5. **未払利息の発生確認（低月額返済）**: 月額返済を極端な低額（例：1万円）に設定した際、即座に「未払利息」の警告UIが表示されること。
6. **最終月の一括清算**: 最終月に残高や未払利息が残る条件にした場合、「⚠️ 最終月（満期）時点の残高・未払利息（一括返済が必要）」の警告と合算額が正しく表示されること。
7. **全体UI検証**: フォーム、テーブル、サマリーなどの必須コンポーネントが常に存在し、レイアウトが保たれていること。

**【UIアサーションの学びと留意点】**
テストスクリプト実行時、一部のDOM取得（例：指定月数の金利がテーブル内に表示されているかの `textContent` チェック）において、**UI側のアコーディオン機能（5年ごとの折り畳みと期間平均金利表示）**によって期待する文字列が隠蔽、またはサマリー値に変換されていることが原因でテストが失敗するケースがありました。
内部の計算ロジック自体は完璧に機能している（一括返済アラートなどが正しく発火している）ため、今後E2Eテストを拡充する際は、アコーディオンの展開アクションを挟むか、描画DOMではなくReactの内部State（または計算エンジンの戻り値）を直接アサートする設計が望ましいです。

### 8-3. 最終ステータス（フェーズ6時点）
- フェーズ6時点では設計上の瑕疵が解消され、基本的な動作検証は完了。ただし、テストアサーションが「存在確認」レベルに留まっており、**計算結果の数値検証・合算整合性・125%ルールの定量検証が不足**していた。
- この不足は後続のフェーズ7で補完された。

## 9. フェーズ7: 網羅的E2Eテスト再実施（2026-06-20）

### 9-1. 背景と目的
フェーズ6（別AI実施）のテストに対する**敵対的・批判的レビュー**を実施した結果、以下の考慮漏れが検出された：
- テストアサーションが「UIの存在確認」レベルに留まり、**計算結果の数値検証がほぼゼロ**
- 5年ルール・125%ルールの**定量的な数値検証が未実施**
- ペアローン合算ロジック（`mergeMonthlyDetails()`）の**整合性チェック未実施**
- 境界値テスト（金利0%、期間1年/50年）が**全て未実施**
- 繰り上げ返済の期間短縮の**定量的検証が不足**（「30年でないこと」の確認のみ）

これらの不足を補完するため、新規にPlaywrightスクリプトを2本作成し、29件の網羅的E2Eテストを実施した。

### 9-2. テストスクリプトと実行環境
- **本テスト**: `tests/e2e/e2e_comprehensive_test.mjs`（26件）
- **追加テスト**: `tests/e2e/e2e_supplementary_test.mjs`（3件）
- **実行環境**: Node.js + Playwright (headless msedge) / Vite dev server (localhost:5173)

### 9-3. テスト結果サマリー

**合計: 29件 | PASS: 29件 | FAIL: 0件**

#### 本テスト（26件）
| ID | シナリオ | 結果 | 検証内容 |
|---|---|:---:|---|
| T-01 | デフォルト値の数値検証 | ✅ | 合算=￥111,497, 夫=￥55,595, 妻=￥55,902（E2E_TEST_PLAN.md期待値と完全一致） |
| T-02 | ペアローン合算一貫性 | ✅ | 夫+妻=合算が支払額・元金・利息・残高すべてで一致。金利欄「-」表示 |
| T-03 | 5年ルール・125%ルール | ✅ | 金利0.5%→5.0%急上昇、125%上限=112,195円、未払利息警告表示 |
| T-04 | 複数段階金利変動 | ✅ | 3段階金利変動（0.68%→0.93%→1.25%→1.20%）でテーブル420行正常描画 |
| T-05 | 固定モード期間短縮 | ✅ | 通常30年→固定100,000円で**16年に短縮** |
| T-06a | monthlyPayment増額 | ✅ | 60,000円で正確に反映 |
| T-06b | monthlyPayment減額 | ✅ | 30,000円→最終月に一括返済必要 |
| T-07 | 未払利息の発生 | ✅ | 3000万/3.0%/月10,000円→未払利息・一括返済警告表示 |
| T-08a | 正常完済パターン | ✅ | デフォルト条件で残高￥18（端数丸め誤差、仕様内） |
| T-08b | 一括精算ありパターン | ✅ | 「合計￥54,734,400の一括返済が必要」正常表示 |
| T-09a | 境界値: 金利0% | ✅ | 利息=0円、支払額=元金充当分が一致 |
| T-09b | 境界値: 期間1年 | ✅ | テーブル12行（12ヶ月以内で完済） |
| T-09c | 境界値: 期間50年 | ✅ | 50年ローンでグラフ正常描画 |
| T-10 | 異期間ペアローン | ✅ | 夫35年+妻20年で合算ロジック正常動作 |
| T-11a〜f | zodバリデーション6件 | ✅ | マイナス値/0年/51年/0円/固定空欄/固定OFF全パターン |
| T-12a | リセット独立性 | ✅ | 夫リセットで妻の値が保持 |
| T-12b | シナリオ追加・削除 | ✅ | 追加2→削除1→残1→再計算成功 |
| T-13 | 固定モード状態管理 | ✅ | OFF→ON→OFF切替で支払額復帰・フィールド表示正常 |
| T-14 | 現実的シナリオ | ✅ | 4000万/35年/0.625%で月額105,820円正常計算 |
| T-15 | UI注釈テキスト | ✅ | 「この値は自動更新されません」表示確認 |
| T-16 | ビルド検証 | ✅ | `tsc -b && vite build` 正常完了 |

#### 追加テスト（3件）— レビュー指摘事項の補完
| ID | シナリオ | 結果 | 検証内容 |
|---|---|:---:|---|
| T-03改 | 125%ルール数値アサーション | ✅ | 61ヶ月目返済額=**112,195円=125%上限ぴったり**。金利5.0%適用、利息105,447円、元金充当6,748円 |
| T-08a改 | 正常完済「✅完済」表示 | ✅ | 固定モード100,000円で期間内完済→「✅完済」表示、残高￥0、未払利息￥0 |
| T-17 | 片方完済後の合算一致 | ✅ | 妻5年目3ヶ月目で完済(残高0)→6年目以降の合算(51,917円)==夫のみ(51,917円) |

### 9-4. 敵対的レビューで検出された設計上の注意点

| 項目 | 深刻度 | 内容 | 対応方針 |
|---|:---:|---|---|
| `calculatePayment()` 金利0%時のゼロ除算 | 低 | `0/0=NaN` が返るが、UIフローでは `initialMonthlyPayment` が常に設定されるため実害なし | 将来のAPI化時にガード追加を推奨 |
| 端数処理による残高￥18残留 | 低 | `Math.round()` の累積丸め誤差。仕様内だがUX改善の余地あり | 閾値導入を検討（セクション5に記載） |
| E2E偏重・ユニットテスト不在 | 中 | 計算ロジックの正確性はユニットテストの方が適切 | 次期タスクで対応予定 |

### 9-5. テストファイルのディレクトリ再編
フェーズ7実施時に、散在していたテスト関連ファイルを `tests/` 配下に整理した。

```
tests/
├── E2E_TEST_PLAN.md              ← テストシナリオ計画書
├── QA_Test_Scenarios.md           ← QAテストシナリオ
├── e2e/                           ← E2Eテストスクリプト群
│   ├── comprehensive_e2e_test.mjs    ← フェーズ6のテスト（別AI作成）
│   ├── e2e_comprehensive_test.mjs    ← フェーズ7 本テスト（26件）
│   ├── e2e_supplementary_test.mjs    ← フェーズ7 追加テスト（3件）
│   └── run_test.js                   ← 初期テストランナー
└── reports/                       ← テスト結果レポート群
    ├── claude-opus-report/
    └── github-copilot-report/
```

### 9-6. フェーズ7の総括
フェーズ7の完了をもってE2Eテストは十分なカバレッジに到達した。次期タスク（フェーズ8）ではユニットテスト作成に移行する。詳細はセクション10を参照。

## 10. ユニットテスト作成プラン（フェーズ8）

### 10-1. 目的と背景

**E2Eテスト29件全PASSにより、アプリケーション全体の結合動作は検証済み。** しかし以下の構造的な問題が残っている：

| 問題 | 影響 |
|---|---|
| 計算ロジックの検証がE2E経由でしかない | UIアコーディオン構造への依存で**脆い**アサーションになる |
| 関数レベルの入出力検証がゼロ | リファクタリング時に**計算の正しさ**を保証できない |
| エッジケースの網羅が困難 | E2Eではフォーム操作コストが高く、**境界値の網羅に限界**がある |

ユニットテストにより、**計算エンジンの正確性をUIに依存せず独立して担保する**ことが目的。

### 10-2. 技術スタック

| ツール | 用途 |
|---|---|
| **Vitest** | テストランナー・アサーション |
| **@testing-library/react** | `usePaymentHistoryHooks` のフックテスト（`renderHook`） |
| **@testing-library/jest-dom** | DOM assertion拡張（必要に応じて） |

#### セットアップ手順
```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

`vite.config.ts` に Vitest 設定を追加：
```ts
/// <reference types="vitest" />
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './tests/unit/setup.ts',
  },
});
```

### 10-3. テスト対象モジュールと関数一覧

| モジュール | 関数 | 種別 | 説明 |
|---|---|:---:|---|
| `src/lib/utils.ts` | `calculatePayment()` | 純粋関数 | 元利均等返済の月額計算 |
| `src/lib/utils.ts` | `formatCurrency()` | 純粋関数 | 金額の日本円フォーマット |
| `src/lib/loanCalculator.ts` | `calculateLoan()` | 純粋関数 | メイン計算エンジン（5年ルール・125%ルール・未払利息） |
| `src/lib/loanCalculator.ts` | `_getRateForMonth()` | 内部関数 | シナリオに基づく月ごとの適用金利取得 |
| `src/lib/loanMerger.ts` | `mergeMonthlyDetails()` | 純粋関数 | ペアローン合算ロジック |
| `src/hooks/usePaymentHistoryHooks.ts` | `calculateSummary()` | フック内関数 | 期間平均・残高要約の計算 |
| `src/hooks/usePaymentHistoryHooks.ts` | `createChunks()` | フック内関数 | 5年(60ヶ月)単位のブロック化 |

> **注意**: `_getRateForMonth()` は非exportの内部関数。テスト方針として (A) `calculateLoan()` 経由で間接テスト、(B) テスト用にexportする のいずれかを選択する。推奨は **(A) 間接テスト**。

### 10-4. テストファイル構成

```
tests/
├── unit/                              ← 新規作成
│   ├── setup.ts                       ← Vitest セットアップファイル
│   ├── utils.test.ts                  ← calculatePayment, formatCurrency
│   ├── loanCalculator.test.ts         ← calculateLoan（メイン、最大のテストファイル）
│   ├── loanMerger.test.ts             ← mergeMonthlyDetails
│   └── usePaymentHistoryHooks.test.ts ← calculateSummary, createChunks
├── e2e/                               ← 既存（変更なし）
│   ├── comprehensive_e2e_test.mjs
│   ├── e2e_comprehensive_test.mjs
│   ├── e2e_supplementary_test.mjs
│   └── run_test.js
├── reports/                           ← 既存（変更なし）
├── E2E_TEST_PLAN.md
└── QA_Test_Scenarios.md
```

### 10-5. テストシナリオ詳細

#### A. `utils.ts` — `calculatePayment()` / `formatCurrency()`

| # | テストケース | 入力 | 期待結果 | E2E対応 |
|---|---|---|---|:---:|
| U-A1 | 標準ケース（夫デフォルト） | 1810万, 0.68%, 360ヶ月 | 55,595円 | T-01 |
| U-A2 | 標準ケース（妻デフォルト） | 1810万, 0.68%, 360ヶ月 | 55,595円 | T-01 |
| U-A3 | 高額・長期ケース | 5000万, 1.5%, 420ヶ月 | 計算値検証 | - |
| U-A4 | 金利0%（ゼロ除算） | 1000万, 0%, 120ヶ月 | **NaN** (※現在の実装) | T-09a |
| U-A5 | 残り0ヶ月 | 1000万, 0.5%, 0ヶ月 | 0 (ガード) | - |
| U-A6 | 残り1ヶ月 | 100万, 1.0%, 1ヶ月 | 元金+利息（≈1,000,833円） | - |
| U-A7 | formatCurrency正常 | 100000 | "￥100,000" | - |
| U-A8 | formatCurrency 0 | 0 | "￥0" | - |

#### B. `loanCalculator.ts` — `calculateLoan()`

##### B-1. 基本動作
| # | テストケース | 条件 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-B1 | デフォルト条件の1ヶ月目 | 1810万, 0.68%, 30年, monthlyPayment=55,595 | 利息=Math.round(1810万×0.68/12/100), 元金=55,595-利息, 残高=1810万-元金 | T-01 |
| U-B2 | デフォルト条件の最終月 | 同上 | 360ヶ月目の残高 ≈ 0〜数十円（端数丸め誤差） | T-08a |
| U-B3 | 全期間の返済額合計 | 同上 | 合計 > 元金（利息分が上乗せ） | - |
| U-B4 | 全期間で残高が単調減少 | 同上 | `details[i].principalBalance >= details[i+1].principalBalance` | - |

##### B-2. initialMonthlyPayment / customMonthlyPayment の優先順位
| # | テストケース | 条件 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-B5 | initialMonthlyPayment指定 | 60,000円指定 | 1ヶ月目の返済額=60,000円、5年ルール通常適用 | T-06a |
| U-B6 | customMonthlyPayment指定 | 100,000円指定 | 1ヶ月目の返済額=100,000円、5年ルール**無効化** | T-05 |
| U-B7 | 両方指定時の優先順位 | initial=60,000, custom=100,000 | customが優先（100,000円） | - |
| U-B8 | どちらも未指定 | 未指定 | calculatePayment()のフォールバック値 | - |

##### B-3. 5年ルール・125%ルール
| # | テストケース | 条件 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-B9 | 見直しタイミング | 3000万, 0.5%→61ヶ月目に5.0% | `details[59].paymentAmount !== details[60].paymentAmount`（60→61で変化） | T-03 |
| U-B10 | 125%上限の数値検証 | 同上 | `details[60].paymentAmount === Math.floor(89756 * 1.25)` = **112,195円** | T-03改 |
| U-B11 | 金利低下時は上限不要 | 3000万, 2.0%→61ヶ月目に0.5% | 61ヶ月目の返済額 < 60ヶ月目の返済額（上限適用なし） | - |
| U-B12 | customMonthlyPayment時は見直しなし | 3000万, 0.5%→5.0%, custom=100,000 | 全期間で返済額=100,000円（見直し無効化） | T-05 |

##### B-4. 未払利息
| # | テストケース | 条件 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-B13 | 未払利息の発生 | 3000万, 3.0%, monthlyPayment=10,000 | 1ヶ月目: 利息=75,000 > 返済額=10,000 → 未払利息=65,000 | T-07 |
| U-B14 | 未払利息の蓄積 | 同上 | `accumulatedUnpaidInterest` が月ごとに増加 | T-07 |
| U-B15 | 未払利息なし（正常） | デフォルト条件 | 全期間で `unpaidInterest === 0` | T-08a |

##### B-5. 最終月・完済ロジック
| # | テストケース | 条件 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-B16 | 最終月の過剰支払い防止 | custom=200,000（大幅増額） | 最終月: `principalPayment <= currentBalance`（直前月の残高以下） | - |
| U-B17 | 早期完済（breakの確認） | custom=200,000 | `details.length < totalMonths`（期間途中で完済） | T-05 |
| U-B18 | 完済時の残高 | custom=200,000 | 最終月の `principalBalance === 0 && accumulatedUnpaidInterest === 0` | T-08a改 |

##### B-6. 境界値
| # | テストケース | 条件 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-B19 | 金利0% + initialMonthlyPayment指定 | 1000万, 0%, 10年, initial=83,334 | 全期間で利息=0、残高が単調減少 | T-09a |
| U-B20 | 期間1年（12ヶ月） | 100万, 0.5%, 1年 | `details.length <= 12` | T-09b |
| U-B21 | 期間50年（600ヶ月） | 3000万, 1.0%, 50年 | `details.length <= 600`、最終残高 ≈ 0 | T-09c |
| U-B22 | 金利変動なし（単一シナリオ） | 1810万, 0.68%, 30年 | 全期間で `interestRate === 0.68` | - |
| U-B23 | 複数段階金利変動 | 0.68%→61m:0.93%→121m:1.25%→181m:1.20% | 各期間で適用金利が正しいこと | T-04 |

#### C. `loanMerger.ts` — `mergeMonthlyDetails()`

| # | テストケース | 入力 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-C1 | 同一期間のマージ | 夫360ヶ月 + 妻360ヶ月 | 全月で `paymentAmount = 夫 + 妻`、`principalBalance = 夫 + 妻` | T-02 |
| U-C2 | 異なる期間のマージ | 夫420ヶ月 + 妻240ヶ月 | 241ヶ月目以降: 合算=夫のみ（妻は0埋め） | T-17 |
| U-C3 | 片方が空配列 | 夫360ヶ月 + 妻[] | 合算 = 夫のデータそのまま | - |
| U-C4 | 両方空配列 | [] + [] | 空配列が返る | - |
| U-C5 | interestRate の Max取得 | 夫0.5% + 妻1.0% | `interestRate = Math.max(0.5, 1.0) = 1.0` | - |

#### D. `usePaymentHistoryHooks.ts` — `calculateSummary()` / `createChunks()`

| # | テストケース | 入力 | 検証ポイント | E2E対応 |
|---|---|---|---|:---:|
| U-D1 | calculateSummary正常 | 12ヶ月分のMonthlyDetail[] | avgPayment, avgInterestRate, endBalance が正確 | - |
| U-D2 | calculateSummary空配列 | [] | null が返る | - |
| U-D3 | createChunks 60ヶ月以下 | 48ヶ月分のデータ | 1ブロック、label="1年目 〜 4年目" | - |
| U-D4 | createChunks ちょうど60ヶ月 | 60ヶ月分のデータ | 1ブロック、label="1年目 〜 5年目" | - |
| U-D5 | createChunks 360ヶ月（30年） | 360ヶ月分のデータ | 6ブロック、各60ヶ月 | - |
| U-D6 | createChunks 端数あり | 90ヶ月分のデータ | 2ブロック: [60ヶ月] + [30ヶ月] | - |
| U-D7 | createChunks ラベル精度 | 13ヶ月分のデータ | label="1年目 〜 2年目"（1ヶ月目〜13ヶ月目） | - |

### 10-6. テストシナリオ総数

| テストファイル | テスト数 |
|---|:---:|
| `utils.test.ts` | 8 |
| `loanCalculator.test.ts` | 23 |
| `loanMerger.test.ts` | 5 |
| `usePaymentHistoryHooks.test.ts` | 7 |
| **合計** | **43** |

### 10-7. 次のAIへの初期プロンプト（依頼文）
> 「`tasks/loan-simulator/HANDOVER.md` のセクション5とセクション10を確認してください。E2Eテスト29件は全PASSで完了済みです。次のタスクとして、セクション10「ユニットテスト作成プラン」に基づき、Vitest + @testing-library/react でユニットテストを作成してください。テスト対象は `utils.ts`、`loanCalculator.ts`、`loanMerger.ts`、`usePaymentHistoryHooks.ts` の4モジュール、合計43件のテストシナリオです。まずセットアップ（Vitest導入、`vite.config.ts` 更新）から始めてください。」
